# HR招聘管理系统 - 功能完善待办计划

> 排查日期：2026-06-12 | 排查范围：前后端全模块  
> 原则：**AI相关功能放最后，先完成基础业务功能**

---

## 一、问题总览

| 优先级 | 数量 | 分类 |
|--------|------|------|
| 🔴 P0-紧急（核心功能不可用） | 7 | 必须立即修复 |
| 🟡 P1-重要（功能不完整） | 5 | 尽快完善 |
| 🟢 P2-优化（体验提升） | 4 | 有余力再做 |
| 🔵 P3-AI功能（最后做） | 4 | AI专项 |

**共计 20 项待办**

---

## 二、P0-紧急（核心功能不可用）

### ✅ P0-01 简历管理「查看」按钮无响应
- **文件**：`frontend/src/views/resume/ResumeListView.vue:231`
- **现状**：`handleDetail(row)` 仅 `ElMessage.info(...)` 显示提示文字
- **预期**：点击"查看"弹出简历详情对话框/抽屉，展示完整信息（基本信息、教育经历、工作经历、技能标签、自我评价、AI匹配分数）
- **涉及修改**：
  - 新建 `ResumeDetailDialog.vue` 组件或直接在当前页添加 `el-drawer` / `el-dialog`
  - 展示字段：姓名、手机号、邮箱、性别、年龄、学历、学校、专业、工作年限、当前公司、当前职位、技能标签、自我评价、AI匹配分数
  - 后端可能需要新增 `GET /api/resumes/{id}` 详情接口

### ✅ P0-02 面试管理「安排」「评价」按钮报错
- **文件**：`frontend/src/views/interview/InterviewListView.vue:29-30`
- **现状**：模板引用了 `handleArrange(row)` 和 `handleEvaluate(row)` 但 script 中**未定义这两个函数**
- **预期**：
  - 「安排」→ 弹出面试安排对话框（修改时间/地点/面试官）
  - 「评价」→ 弹出面试评价对话框（评分+评语+AI摘要生成）
- **涉及修改**：
  - 前端：添加两个 dialog 和对应函数
  - 后端：`InterviewController.submitEvaluation()` 当前为空实现（`:42`），需保存评价数据到 `interview_evaluation` 表

### ✅ P0-03 后端面试评价提交为空实现
- **文件**：`backend/.../controller/InterviewController.java:40-43`
- **现状**：`submitEvaluation()` 直接返回 `"提交成功"`，不保存任何数据
- **预期**：接收评价数据 → 存入 `interview_evaluation` 表 → 更新面试状态为 COMPLETED
- **需要确认**：数据库是否有 `interview_evaluation` 表？如果没有需要创建

### ✅ P0-04 后端面试摘要生成为空实现
- **文件**：`backend/.../controller/InterviewController.java:45-48`
- **现状**：`getSummary()` 返回硬编码 `"暂无面试摘要"`
- **预期**：查询该面试的评价记录 → 调用AI生成摘要 → 返回摘要文本
- **注意**：AiController 已有 `summarizeEvaluation()` 方法可复用

### ✅ P0-05 候选人「我的申请」使用模拟数据
- **文件**：`frontend/src/views/candidate/CandidateMyApplicationsView.vue:77-90`
- **现状**：`loadApplications()` 使用 `mockData` + `setTimeout` 模拟，真实API被注释掉
- **预期**：调用后端 `GET /api/candidate/applications?keyword=xxx` 获取真实数据
- **涉及修改**：
  - 取消注释真实API调用（但需传参：邮箱或手机号）
  - 由于候选人门户不登录，考虑用URL参数或session传递身份信息
  - 后端 `CandidateController.getMyApplications()` 已实现，需确认接口对接

### ✅ P0-06 Offer发送功能不完整
- **文件**：`backend/.../controller/OfferController.java:38-46`
- **现状**：仅更新状态为 SENT + 记录发送时间，缺少：
  - 生成Offer文件（PDF/Word）
  - 发送通知给候选人
  - 更新关联的 applicant_job 状态
- **前端**：`OfferView.vue` 的 `handleSend(row)` 函数也未定义
- **预期**：至少完成状态更新+通知发送，文件生成可作为P1

### ✅ P0-07 简历上传后未触发AI解析
- **文件**：`frontend/src/views/resume/ResumeListView.vue:222-224`
- **现状**：`onUploadSuccess()` 只显示成功消息+刷新列表，**未调用 `parseResume` API**
- **预期**：上传成功后自动调用 `POST /api/resumes/{id}/parse` 触发PDF解析
- **后端**：`ResumeInfoServiceImpl.parseResume()` 已在上轮实现（PDFBox+AI），需确认能正常工作

---

## 三、P1-重要（功能不完整）

### ✅ P1-01 招聘管道阶段更新逻辑不完整
- **✅ 已完成**（2026-06-13）

### ✅ P1-02 面试安排未保存面试官关联
- **✅ 后端已完成**（2026-06-13）
- **⚠️ 前端待修复**（2026-06-13）：
  - 问题1：表格操作列的"安排"按钮显示在 `status==='SCHEDULED'` 时，但 SCHEDULED 表示已安排，应该显示"重新安排"或改为 `status==='PENDING'`
  - 问题2："评价"按钮显示在 `status==='COMPLETED'` 时，但逻辑上应该在面试完成后（ONGOING）才能评价，COMPLETED 表示已评价完成
  - 问题3：缺少"新建面试"按钮
  - 修复：调整按钮显示逻辑 + 添加新建面试功能

### ✅ P1-03 AI匹配评分页面为空
- **✅ 已完成**（2026-06-13）

### ✅ P1-04 AI中心缺少「生成面试题」标签页
- **✅ 已完成**（2026-06-13）

### ✅ P1-05 Offer管理页面功能缺失
- **✅ 已完成**（2026-06-13）

---

## 三、P1-补充（新发现的问题）

### 📋 P1-06 面试管理前端按钮逻辑错误
- **文件**：`frontend/src/views/interview/InterviewListView.vue:29-30`
- **问题**：
  1. "安排"按钮在 `status==='SCHEDULED'` 时显示（逻辑错误，SCHEDULED 表示已安排）
  2. "评价"按钮在 `status==='COMPLETED'` 时显示（逻辑错误，COMPLETED 表示已评价完成）
  3. 缺少"新建面试"按钮
- **修复方案**：
  - "安排"按钮改为在 `status==='PENDING'` 或 `status==='SCHEDULED'` 时显示
  - "评价"按钮改为在 `status==='ONGOING'` 或面试时间已过时显示
  - 添加"新建面试"按钮和弹窗

---

## 四、P2-优化（体验提升）

### ✅ P2-01 Dashboard统计卡片图标问题
- **文件**：`frontend/src/views/dashboard/DashboardView.vue:47-51`
- **现状**：icon 使用字符串 `'Briefcase'` `'Document'` 等，应为实际引入的 icon 组件
- **✅ 已完成**（2026-06-13）：
  - 从 `@element-plus/icons-vue` 导入 `Briefcase, Document, ChatDotRound, Checked`
  - `statCards` 中的 `icon` 改为组件引用（而非字符串）
  - 模板中使用 `<component :is="item.icon" />` 渲染

### ✅ P2-02 候选人Offer查看未实现
- **文件**：`frontend/src/views/candidate/CandidateMyApplicationsView.vue:116-118`
- **现状**：`viewOffer(app)` 仅显示"功能开发中"
- **✅ 已完成**（2026-06-13）：
  - 新增 Offer 详情弹窗（ElDialog）
  - 调用 `getOfferDetail(offerId)` API 获取详情
  - 显示岗位名称、部门、薪资、入职时间、福利待遇、备注说明
  - 添加成功提示（恭喜获得Offer）
  - 添加"确认接受"按钮（待实现后端接口）

### ✅ P2-03 操作日志筛选条件优化
- **文件**：`frontend/src/views/log/LogView.vue`
- **现状**：已有基本筛选，但缺少时间范围选择
- **✅ 已完成**（2026-06-13）：
  - 前端：添加 `el-date-picker` 时间范围选择器
  - 前端：添加"重置"按钮
  - 前端：添加分页组件
  - 后端：`OperationLogController.list()` 新增 `startTime` 和 `endTime` 参数支持
  - 后端：添加时间范围查询条件（`ge` + `le`）
  - 后端：添加 `@Slf4j` 注解和日期格式异常处理

### ✅ P2-04 简历管理编辑字段不全
- **文件**：`frontend/src/views/resume/ResumeListView.vue`
- **现状**：编辑弹窗只有姓名/手机号/学历/工作年限/技能标签
- **✅ 已完成**（2026-06-13）：
  - 编辑弹窗新增字段：邮箱、性别、学校、专业、当前公司、当前职位、自我评价
  - `editForm` 对象新增对应字段
  - `handleEdit()` 函数新增字段填充
  - `handleEditSubmit()` 函数新增字段提交
  - 弹窗宽度从 600px 调整为 680px

---

## 五、P3-AI功能（最后完成）

> 以下功能依赖DeepSeek API稳定运行，放在基础功能全部完成后进行

### 🔵 P3-01 AI知识库/RAG检索增强
- 基于 `AI功能优化与知识库建设方案.md` 实现
- 将简历、岗位数据向量化存储
- 实现检索增强生成（RAG）

### 🔵 P3-02 AI智能推荐
- 相似简历推荐
- 岗位智能推荐
- 简历质量评分

### 🔵 P3-03 AI数据分析洞察
- 招聘漏斗分析
- 简历质量分布
- 招聘周期预测

### 🔵 P3-04 AI面试辅助增强
- 实时面试纪要（语音转文字→AI提炼要点）
- 面试报告自动生成
- 多维度面试评分建议

---

## 六、执行计划（建议顺序）

```
第一阶段：核心业务功能打通（P0 全部）
├── P0-01 简历查看详情          [预计 30min]
├── P0-02 前端面试安排/评价弹窗   [预计 40min]
├── P0-03 后端面试评价保存        [预计 30min]
├── P0-04 后端面试摘要生成        [预计 20min]
├── P0-05 候选人我的申请真实API    [预计 20min]
├── P0-06 Offer发送完整流程       [预计 30min]
└── P0-07 上传后自动解析           [预计 15min]

第二阶段：功能完善（P1 全部）
├── P1-01 管道状态机+自动流转      [预计 40min]
├── P1-02 面试安排同步面试官        [预计 20min]
├── P1-03 AI匹配评分页面          [预计 30min]
├── P1-04 AI生成面试题标签         [预计 25min]
└── P1-05 Offer管理完善           [预计 30min]

第三阶段：体验优化（P2 按需）
└── P2-01 ~ P2-04                [预计 60min]

第四阶段：AI增强（P3 最后）
└── P3-01 ~ P3-04                [根据剩余时间]
```

---

## 七、已修复问题记录（历史故障）

以下问题已在之前的排查中修复，供参考：

| 故障ID | 描述 | 状态 |
|--------|------|------|
| 014 | 前后端状态值不匹配（OPEN vs PUBLISHED） | ✅ |
| 015 | CandidateController类型转换错误 | ✅ |
| 016 | JobPositionService状态值错误 | ✅ |
| 017 | 前端候选人API路径错误 | ✅ |
| 018 | EventSource无法传递JWT Token | ✅ |
| 019 | 简历管理需手动查询 | ✅ |
| 020 | 招聘管道API路径重复 | ✅ |
| 021 | 通知中心功能不完善 | ✅ |
| 022 | 操作日志报错 | ✅ |
| 023 | 候选人页面UI需优化 | ✅ |
| 024 | 岗位状态显示+投递按钮 | ✅ |

---

## 八、关键文件索引

### 前端核心文件
```
hr-recruit-frontend/src/
├── api/                    # 所有API定义
│   ├── request.js          # axios封装（baseURL: /api）
│   ├── resume.js           # 简历CRUD + 解析
│   ├── interview.js        # 面试列表
│   ├── pipeline.js         # 招聘管道
│   ├── offer.js            # 录用管理
│   ├── ai.js               # AI相关（JD生成/匹配）
│   ├── job.js              # 岗位管理
│   ├── notification.js     # 通知
│   └── common.js           # 操作日志
├── views/
│   ├── resume/ResumeListView.vue        # 简理管理（查看/编辑待实现）
│   ├── interview/InterviewListView.vue  # 面试管理（安排/评价未定义）
│   ├── pipeline/PipelineView.vue        # 招聘管道看板
│   ├── offer/OfferView.vue              # 录用管理（send未定义）
│   ├── ai/AiCenterView.vue              # AI中心（匹配评分为空）
│   └── candidate/
│       ├── CandidateMyApplicationsView.vue  # 我的申请（模拟数据）
│       └── CandidateApplyView.vue           # 投递页面
└── router/index.js         # 路由配置
```

### 后端核心文件
```
hr-recruit-backend/src/main/java/com/hr/recruit/
├── controller/
│   ├── ResumeController.java      # 简历CRUD
│   ├── InterviewController.java   # 面试管理（评价/摘要空实现）
│   ├── OfferController.java       # 录用管理（send不完整）
│   ├── AiController.java          # AI功能（已实现JD/匹配/题/聊天）
│   ├── CandidateController.java   # 候选人投递（已实现）
│   ├── RecruitmentPipelineController.java  # 管道（状态更新简单）
│   └── NotificationController.java        # 通知
├── service/impl/
│   └── ResumeInfoServiceImpl.java  # PDF解析（上轮已实现PDFBox+AI）
└── entity/                        # 实体类
```

---

*本文档将持续更新，每完成一项打 ✅ 并记录完成时间*
